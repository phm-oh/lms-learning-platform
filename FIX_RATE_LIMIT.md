# 🔧 แก้ไขปัญหา Rate Limit (429 Too Many Requests)

## ปัญหา
เมื่อพยายาม login ได้ error `429 Too Many Requests` เพราะ rate limiting จำกัด 5 requests ต่อ 15 นาที

## วิธีแก้ไข

### วิธีที่ 1: Restart Backend Server (แนะนำ)
Rate limit เก็บข้อมูลใน memory ดังนั้น restart server จะ clear cache:

```bash
# หยุด backend server (Ctrl+C)
# แล้วรันใหม่
cd backend
npm run dev
```

### วิธีที่ 2: รอ 15 นาที
Rate limit window คือ 15 นาที ถ้ารอ 15 นาทีจะสามารถ login ได้อีกครั้ง

### วิธีที่ 3: ใช้ IP อื่น
ถ้าใช้ network อื่นหรือ VPN จะได้ IP ใหม่

---

## สิ่งที่แก้ไขแล้ว

### 1. เพิ่ม Rate Limit ใน Development Mode
- Development: 50 requests ต่อ 15 นาที (เพิ่มจาก 5)
- Production: 5 requests ต่อ 15 นาที (เดิม)

### 2. Skip Rate Limit สำหรับ Localhost
- ใน development mode, localhost (127.0.0.1) จะไม่ถูก rate limit

---

## วิธีป้องกันในอนาคต

1. **ใช้ credentials ที่ถูกต้อง** - login ผิดหลายครั้งจะโดน block
2. **Restart server** - ถ้าโดน block ให้ restart backend
3. **ตรวจสอบ .env** - ตรวจสอบว่า `NODE_ENV=development` ถูกตั้งค่าไว้

---

## หมายเหตุ

- Rate limiting ใช้ memory store (default) ดังนั้น restart server จะ clear cache
- ถ้าต้องการ persistent rate limiting ต้องใช้ Redis store
- ใน production ควรใช้ Redis store เพื่อป้องกัน distributed rate limit bypass


