# 🔧 แก้ไขปัญหา Admin Login

## ปัญหาที่พบ

1. **403 Forbidden** - "บัญชีของคุณยังไม่ได้รับการอนุมัติ"
   - บัญชีมี status = 'pending' หรือ 'suspended'
   - ต้องเปลี่ยน status เป็น 'active'

2. **401 Unauthorized** - "อีเมลหรือรหัสผ่านไม่ถูกต้อง"
   - Email หรือ password ไม่ถูกต้อง
   - หรือบัญชีไม่มีในฐานข้อมูล

## วิธีแก้ไข

### วิธีที่ 1: สร้าง Admin Account ใหม่ (แนะนำ)

รันคำสั่งนี้ใน backend directory:

```bash
cd backend
npm run db:create-admin
```

หรือรันโดยตรง:

```bash
node src/utils/createAdmin.js
```

**Credentials:**
- Email: `admin@lms-platform.com`
- Password: `admin123`

### วิธีที่ 2: ใช้ Seed Database

รันคำสั่งนี้เพื่อ seed ข้อมูลทั้งหมด (รวม admin account):

```bash
cd backend
npm run db:seed
```

**Credentials:**
- Email: `admin@lms-platform.com`
- Password: `admin123`

### วิธีที่ 3: สร้าง Admin Account ผ่าน Database

ถ้าต้องการสร้าง admin account ผ่าน database โดยตรง:

```sql
-- เช็คว่ามี admin อยู่แล้วหรือไม่
SELECT id, email, role, status FROM users WHERE email = 'admin@lms-platform.com';

-- ถ้ายังไม่มี สร้างใหม่ (password จะถูก hash อัตโนมัติ)
-- หรือถ้ามีแล้ว แต่ status ไม่ใช่ 'active' ให้ update
UPDATE users 
SET status = 'active', role = 'admin' 
WHERE email = 'admin@lms-platform.com';
```

### วิธีที่ 4: สร้าง Admin Account ผ่าน Register API

1. สมัครสมาชิกใหม่ด้วย role = 'admin' (ต้องแก้ไข backend ให้รองรับ)
2. หรือใช้ SQL เพื่อเปลี่ยน role ของ user ที่มีอยู่แล้ว

## ตรวจสอบ Admin Account

### ตรวจสอบผ่าน Database

```sql
SELECT id, email, first_name, last_name, role, status, email_verified 
FROM users 
WHERE role = 'admin';
```

### ตรวจสอบผ่าน API

```bash
# Login เพื่อทดสอบ
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "admin@lms-platform.com",
    "password": "admin123"
  }'
```

## Troubleshooting

### ปัญหา: "บัญชีของคุณยังไม่ได้รับการอนุมัติ"

**สาเหตุ:** Admin account มี status = 'pending' หรือ 'suspended'

**แก้ไข:**
```sql
UPDATE users 
SET status = 'active' 
WHERE email = 'admin@lms-platform.com' AND role = 'admin';
```

### ปัญหา: "อีเมลหรือรหัสผ่านไม่ถูกต้อง"

**สาเหตุ:**
1. Email หรือ password ไม่ถูกต้อง
2. บัญชีไม่มีในฐานข้อมูล
3. Password ถูก hash ผิด

**แก้ไข:**
1. ตรวจสอบว่า admin account มีในฐานข้อมูลหรือไม่
2. ใช้ script `createAdmin.js` เพื่อสร้างใหม่
3. หรือ reset password ผ่าน database

### ปัญหา: Password ไม่ตรง

**สาเหตุ:** Password ถูก hash แล้ว แต่เราใส่ plain text

**แก้ไข:** ใช้ script `createAdmin.js` ซึ่งจะ hash password ให้อัตโนมัติ

## ข้อมูล Admin Account เริ่มต้น

หลังจากรัน seed หรือ createAdmin script:

- **Email:** `admin@lms-platform.com`
- **Password:** `admin123`
- **Role:** `admin`
- **Status:** `active`
- **Email Verified:** `true`

## หมายเหตุ

- Admin account จะถูก hash password อัตโนมัติโดย `beforeCreate` hook ใน User model
- Script `createAdmin.js` จะตรวจสอบว่ามี admin อยู่แล้วหรือไม่ ถ้ามีจะ update status เป็น 'active'
- ถ้าต้องการเปลี่ยน password ให้ใช้ API `/api/auth/change-password` หรือแก้ไขผ่าน database



